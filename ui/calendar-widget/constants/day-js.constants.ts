import dayjs from 'dayjs';

export const INITIAL_YEAR = Number(dayjs().format('YYYY'));
export const INITIAL_MONTH = Number(dayjs().format('M'));
export const INITIAL_DAY = Number(dayjs().format('D'));
